version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    restart: always
    environment:
      - NODE_ENV=production
    # No volumes needed; leads.json is persisted in container (acceptable for MVP)
